/**
 * Copy to analytics.local.ts and set your PostHog project API key (phc_...).
 * analytics.local.ts is gitignored — never commit real keys.
 *
 * Or set UNTIL_POSTHOG_API_KEY in .env (loaded by babel + dotenv).
 */
export const POSTHOG_API_KEY = '';
export const POSTHOG_HOST = 'https://us.i.posthog.com';
/** Required for yarn ios / yarn android — PostHog is off in __DEV__ otherwise. */
export const POSTHOG_DEV_ENABLED = true;
