/**
 * iOS native module interface (JS side)
 * Actual Swift implementation lives in ios/UNTIL/
 * Use this file for iOS-specific bridge types if needed
 */

export { platformBridge } from '../index';
